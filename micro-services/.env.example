# AWS credentials dedicated to the Synvia GIG importer micro-service.
# Replace the placeholder values below with secrets scoped to a least-privilege IAM user or role.

# Access keys used by the AWS SDK v3 client.
AWS_ACCESS_KEY_ID=REPLACE_ME
AWS_SECRET_ACCESS_KEY=REPLACE_ME

# Region where the target S3 bucket lives (e.g. sa-east-1).
AWS_DEFAULT_REGION=sa-east-1

# Bucket that stores both the uploaded CSVs (imports/) and the structured logs (logs/).
AWS_BUCKET=synvia

# Port exposed by the Express server. Keep 3001 in Docker compose to avoid collisions with the front-end dev server.
PORT=3001